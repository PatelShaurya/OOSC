{
  "name": "civicai",
  "version": "1.0.0",
  "private": true,
  "description": "CivicAI Platform - Action-Oriented Civic Guidance & RTI Agent",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npx vite build",
    "vercel-build": "cd frontend && npm install --legacy-peer-deps && npx vite build",
    "start": "cd frontend && npm run start"
  }
}
